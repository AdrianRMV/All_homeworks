 //BROCHASOS

    int metrosc,precio,totalA,total;

    cout<<"Cuantos metros desea pintar? "; cin>>metrosc;

    if (metrosc < 4 ){
        cout<<"Lo sentimos, solo hacemos trabajos mayores a 4 m2";

    }
    if (metrosc > 40){
        cout<<"Se le avisa que para este trabajo tendrá un aumento del 40% por ser doble trabajador, Gracias"<<endl;
        cout<<"Ingrese el precio del precio del m2: "; cin>>precio;
        total = metrosc * precio;
        totalA = total * 0.40;
        total += totalA;
        cout<<"El precio a pagar es de: $"<<total;

    }
    else{
        cout<<"Ingrese el precio del precio del m2: "; cin>>precio;
        total = metrosc * precio;
        cout<<"El precio a pagar es de: $"<<total;

    }
}
