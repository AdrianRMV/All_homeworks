//AUTOBUSES KILOMETROS

    int km,costo,tarifa,desc;

    cout<<"Cuantos kilometros desea viajar? "; cin>>km;
    cout<<"ingrese el costo del km: $"; cin>>costo;

    if (km<50){
        costo = costo*km;
        tarifa = costo * 0.15;
        costo += tarifa;
        cout<<"El total es de: $"<<costo;

    }
    if (km > 100){
        costo = costo*km;
        desc = costo * 0.20;
        costo -= desc;
        cout<<"El total es de: $"<<costo;

    }


}
